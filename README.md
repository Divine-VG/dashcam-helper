# Traffic detection
The code processes the video by slicing and then detecting the types of vehicles, only when a vehicle crosses the yellow line it will be counted 
The code is mainly based on YOLO(a pretrained AI used for object detection)
